metadata:
  kind: gitlab-endpoint
  name: test-gitlab-endpoint
  labels:
    gitlab: webhook
data:
  defaultCommand:
    noun: echo
    verb: create
  hookUser: 1b7b1e62-1753-4d10-9546-065e86376f4c
  transforms:
    - metadata:
        kind: template-transform
        name: gitlab-webhook-template
      data:
        templates:
          kind: "{{ object_kind }}"
          ref: "{{ ref }}"
          project: "{{ project.[0].name }}"
          user: "{{ user_name }}"

