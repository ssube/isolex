metadata:
  kind: gitlab-endpoint
  name: test-gitlab-endpoint
  labels:
    gitlab: webhook
data:
  defaultCommand:
    noun: echo
    verb: create
  hookUser: 07689156-5395-480d-b135-1649656642f5
  transforms:
    - metadata:
        kind: template-transform
        name: gitlab-webhook-template
      data:
        templates:
          kind: "{{ object_kind }}"
          ref: "{{ ref }}"
          project: "{{ project.name }}"
          user: "{{ user_name }}"

