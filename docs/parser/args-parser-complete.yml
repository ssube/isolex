metadata:
  kind: args-parser
  name: default-complete
data:
  args:
    required:
      - id
      - next
  emit:
    noun: fragment
    verb: update
  match:
    rules:
      - key: body
        operator: every
        values:
          - regexp: !regexp /!!c/