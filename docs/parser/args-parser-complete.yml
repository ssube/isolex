metadata:
  kind: args-parser
  name: default-complete
data:
  args:
    required:
      - id
      - next
  defaultCommand:
    noun: fragment
    verb: update
  match:
    rules:
      - key: body
        operator: every
        values:
          - regexp: !regexp /!!c/