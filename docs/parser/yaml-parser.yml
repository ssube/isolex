metadata:
  kind: yaml-parser
  name: default-yaml-parser
data:
  match:
    rules:
      - key: body
        operator: any
        values:
          - regexp: !regexp /!!json/
          - regexp: !regexp /!!yaml/