metadata:
  kind: args-parser
  name: auth_login
data:
  args:
    required:
      - name
  defaultCommand:
    noun: session
    verb: create
  match:
    rules:
      - key: body
        operator: every
        values:
          - regexp: !regexp /!!login/