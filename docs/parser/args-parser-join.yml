metadata:
  kind: args-parser
  name: auth_join
data:
  args:
    array:
      - roles
    required:
      - name
  emit:
    noun: user
    verb: create
  match:
    rules:
      - key: body
        operator: every
        values:
          - regexp: !regexp /!!join/