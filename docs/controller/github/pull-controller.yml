metadata:
  kind: github-pull-controller
  name: test-pull-controller
data:
  client:
    agent: ssube/isolex/controller/github/pull-request
    app:
      id: !env ISOLEX_GITHUB_APP_ID
      key: !env ISOLEX_GITHUB_APP_KEY
    installation:
      id: !env ISOLEX_GITHUB_APP_INSTALLATION

  filters: []
  transforms: []