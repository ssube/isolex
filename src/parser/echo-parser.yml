$schema: "http://json-schema.org/schema#" # yes, this is a json schema written in yaml
$id: "https://github.com/ssube/isolex/echo-parser.yml"
type: object
allOf:
  - $ref: "#/definitions/base-parser"
  - properties:
      args:
        type: object
        properties:
          field:
            type: string
          remove:
            type: boolean